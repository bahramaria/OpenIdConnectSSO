name: .NET

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore OpenIdConnectSSO.sln

      - name: Build
        run: dotnet build OpenIdConnectSSO.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test OpenIdConnectSSO.sln --configuration Release --no-build --verbosity normal
